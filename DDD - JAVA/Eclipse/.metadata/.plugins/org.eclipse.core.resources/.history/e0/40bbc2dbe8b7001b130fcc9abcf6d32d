package challenge;

import java.text.SimpleDateFormat;

public class Teste {
	static SimpleDateFormat sdf = new SimpleDateFormat("yyyyMMddHHmmssSSSS");
	
	private static long randomId() {
	    String id = sdf.format(System.currentTimeMillis());
	    long numero = Long.valueOf(id).longValue();
	    return numero;
	}
	 
	
	public static void main(String[] args) {
		Usuario user1 = new Usuario(randomId(), "email@email.com", "123456", "João", "Da Silva",
				"15/05/1995", "masculino", "11 99999-4444", "Usuário Comum");
		Chat chat = new Chat(user1.getUserId(), randomId(), "Teste");
		Publicacao publicacao = new Publicacao(user1.getUserId(), randomId(), "17/05/2021", "relato", "Feliz", "Conteúdo");
		Comentario coment = new Comentario(user1.getUserId(), randomId(), "21/05/2021", "comentário");
		

		System.out.println(user1.getUserId());
		System.out.println(chat.getChatId());
		System.out.println(publicacao.getPubId());
		System.out.println(coment.getComentId());
		
		
		
	}	
}
